<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hxzz.demo.mapper.ScglMapper">
    <select id="show" resultType="com.hxzz.demo.entity.Scgl">
  select * from Quality where date=(select date from Quality where id=(select MAX(id) as id from Quality)) order
         by name,id DESC

   </select>
    <select id="showClient" resultType="com.hxzz.demo.entity.Scgl">
select name,sum(offLine) as offLine,sum(dLine) as dLine,sum(lingYu) as lingYu,sum(careLine) as careLine,sum(roadTest) as roadTest,sum(DR) as DR,date from Quality  where date=(select
				  date from Quality where id=(select MAX(id) as id from Quality))  group by date,name

    </select>
    <select id="showSum" resultType="com.hxzz.demo.entity.Scgl">

select sum(offLine) as offLine,sum(dLine) as dLine,sum(lingYu) as lingYu,sum(careLine) as careLine,sum(roadTest) as roadTest,sum(DR) as DR,date from Quality  where date=(select
				  date from Quality where id=(select MAX(id) as id from Quality))  group by date
    </select>
    <select id="info" resultType="com.hxzz.demo.entity.Scgl">
         select * from Quality where date>=#{date1} and date &lt;= #{date2} order by name,id DESC

    </select>
    <select id="infoClient" resultType="com.hxzz.demo.entity.Scgl">
       select name,sum(offLine) as offLine,sum(dLine) as dLine,sum(lingYu) as lingYu,sum(careLine) as careLine,sum(roadTest) as roadTest,sum(DR) as DR from Quality where (date&gt;=#{date1} and date&lt;= #{date2})  group by name
    </select>
    <select id="infoSum"  resultType="com.hxzz.demo.entity.Scgl">
select sum(offLine) as offLine,sum(dLine) as dLine,sum(lingYu) as lingYu,sum(careLine) as careLine,sum(roadTest) as roadTest,sum(DR) as DR from Quality where (date&gt;=#{date1} and date&lt;= #{date2})

    </select>
    <insert id="add" >
    insert into Scgl( name,targetCapacity,  actualCapacity,  targetEfficiency,  workingHours, beat)
    values(#{name},#{targetCapacity},#{actualCapacity},#{targetEfficiency},#{workingHours},#{beat})
</insert>
    <delete id="del">
    delete  from Scgl where id=#{id}
</delete>
    <update id="change" >
    update Scgl set  name=#{name},targetCapacity=#{ targetCapacity}, actualCapacity=#{actualCapacity},
     targetEfficiency=#{targetEfficiency}, workingHours=#{ workingHours}, beat=#{beat}  where id=#{id}
</update>
</mapper>
